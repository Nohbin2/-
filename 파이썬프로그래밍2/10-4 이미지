from tkinter import *
window = Tk()

photo = PhotoImage(file = "C:/Users/Cju/Desktop/images/GIF/dog.gif") ##파일 경로 / ##
label1 = Label(window, image = photo) ##이미지는 속성 벨류 포토 ##

label1.pack()

window.mainloop()
